<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CentralInovacao.Pages.PageRankingGeral"
             BackgroundImageSource="ranking.png"
             Title=" ">

    <VerticalStackLayout>
        <Grid 
            RowDefinitions="Auto,Auto" 
            BackgroundColor="#14d1ca">
            <Label
                Grid.Row="0"
                Text="Seu Rank"
                FontSize="35"
                TextColor="White"
                FontAttributes="Bold"
                HorizontalTextAlignment="Center">
                <Label.Shadow>
                    <Shadow 
                        Brush   = "Black"
                        Offset  = "5,5"
                        Radius  = "1"
                        Opacity = "0.5"/>
                </Label.Shadow>
            </Label>
            <Label
                Grid.Row="1"
                Text="1º"
                TextColor="White"
                FontSize="100"
                FontAttributes="Bold"
                HorizontalTextAlignment="Center">
                <Label.Shadow>
                    <Shadow 
                        Brush   = "Black"
                        Offset  = "5,5"
                        Radius  = "1"
                        Opacity = "0.5"/>
                </Label.Shadow>
            </Label>
        </Grid>
    </VerticalStackLayout>
</ContentPage>